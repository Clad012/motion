import { createNamedSfx } from "@/generic/components/audio";
import { SFX } from "../brand";

// Named Yuniqa sound effect: <Sfx name="pop" from={12} />. Names and default levels live in SFX (brand.ts).
export const Sfx = createNamedSfx(SFX);
